# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
# github cd がダサいとき
# https://blog.takuchalle.dev/post/2020/02/20/github_actions_change_directory/

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs: # 各ジョブは仮想環境の新しいインスタンスで実行される。
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    env:
      RUN_HOME: 'portfolio-ui'
    runs-on: ubuntu-latest
    steps:
      - name: Env Info.
        run: echo ${{ env.RUN_HOME }}

      - uses: actions/checkout@v2

      - name: Setup node.js.
        uses: actions/setup-node@v2
        with:
          node-version: '14.15.4'
          cache: 'yarn'
          cache-dependency-path: ${{ env.RUN_HOME }}/yarn.lock

      # どうやら-nameごとで階層は共有されないらしい
      - name: Install module. & Bundle file create.
        run: yarn install --frozen-lockfile && yarn build:hosting
        working-directory: ./${{ env.RUN_HOME }}

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_VIDE_PRD }}'
          projectId: vide-prd
          path: ./${{ env.RUN_HOME }}

